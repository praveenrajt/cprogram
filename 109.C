#include<stdio.h>
#include<conio.h>
int main()
{
  int i,a[100],max,no;
  
  printf("\n Enter the elements=");
  for(i=0;i<10;i++)
  {
    scanf("%d",&a[i]);
  }
  max=a[0];
  for(i=0;i<n;i++)
  {
    if(a[i]<max)
    {
      max=a[i];
      no=a[i];
    }
  }
  printf("\n The min no is=%d",no);
  getch();
}
